// Shared configuration for Nav Med AI platform

// Environment configuration
export { default as config } from './config';
export { validateConfig } from './validation';

// Constants
export * from './constants';

// Environment types
export * from './types';
